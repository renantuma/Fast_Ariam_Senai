# TODO - Melhorias HTML/CSS (Fast Ariam)

- [x] Levantar escopo: corrigir HTML/CSS em **todos** os arquivos `.html` do projeto (templates)

- [x] Consolidar CSS: remover `<style>` gigante do `fragments/layout.html` e migrar para `static/css/main.css`

- [ ] Criar/ajustar classes reutilizáveis em `main.css` para substituir `style="..."` (cards gradiente, dropzone, chips/badges, spacing, mobile button, overlay etc.)
- [x] Melhorar `pedidos/novo.html`: substituir `style="..."` por classes, melhorar acessibilidade e consistência

- [ ] Melhorar demais templates: `dashboard.html`, `login.html`, `pedidos/*.html`, `admin/*.html` e fragments (sem alterar lógica)
- [ ] Garantir compatibilidade: manter classes existentes (`btn-fa`, `fa-card`, `fa-table`, `step-bar`, etc.)
- [x] Rodar build/test do projeto (mvn test)


